<!-- añadimos 'cl_image_tag', ojo, hacemos lo de 'cl' para conectar con claudinary: pd tbn podemos añadir
width and height gracias a claudinary -->
<%= cl_image_tag("ozx3doygq5rxuurxetko",
      width: 400, height: 300, crop: :fill) %>

      <%= cl_image_tag("qd2lujxd5fmwdzhd4eyc",
      width: 400, height: 300, crop: :fill) %>

      <!-- for face detection -->
<%= cl_image_tag("qd2lujxd5fmwdzhd4eyc",
      width: 150, height: 150, crop: :thumb, gravity: :face) %>

      <%= cl_image_tag("qd2lujxd5fmwdzhd4eyc",
      width: 200, height: 300, crop: :thumb, gravity: :face) %>
<!-- solo para saber un poco mas de los diferentes crops -->
      <%= cl_image_tag("qd2lujxd5fmwdzhd4eyc",
      width: 150, height: 200, crop: :pad) %>
      <!-- para card with background  -->

      <!-- probando resolver problema con `cl`, not working -->

      <%= cl_image_path("qd2lujxd5fmwdzhd4eyc",
      width: 150, height: 200, crop: :pad) %>



<p id="notice"><%= notice %></p>

<h1>Articles</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= article.title %></td>
        <td><%= article.body %></td>
        <td><%= link_to 'Show', article %></td>
        <td><%= link_to 'Edit', edit_article_path(article) %></td>
        <td><%= link_to 'Destroy', article, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<!--añadiendo card with background with cloudinary img -->

<div class="card"
  style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)),
         url('<%= cl_image_path @article.photo, height: 300, width: 400, crop: :fill %>')">
  <div class="card-category">Popular</div>
  <div class="card-description">
    <h2>Some stuff</h2>
    <p>Very cool stuff, useful and smart</p>
  </div>
  <a class="card-link" href="#" ></a>
</div>

<%= link_to 'New Article', new_article_path %>
